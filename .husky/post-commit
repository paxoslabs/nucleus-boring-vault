sig_status=$(git log --format='%G?' -1 HEAD)

if [ "$sig_status" = "N" ]; then
    echo "Error: the commit you just created is not signed."
    echo "Please amend it with: git commit --amend -S"
    exit 1
fi
